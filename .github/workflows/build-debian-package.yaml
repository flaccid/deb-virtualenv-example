name: Build helloart Debian package
run-name: Build helloart Debian package by ${{ github.actor }}
on: [push]
jobs:
  build:
    container:
      image: debian:bookworm
    #runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get -y install \
            debhelper \
            dh-python \
            dh-virtualenv \
            dpkg-dev \
            make \
            python3-dev \
            python3-pip \
            python3-venv \
            python-is-python3 \
            python3-setuptools
            tree
      - name: Build debian package
        run: |
          cd ${{ github.workspace }}
          make build-deb
          make copy-dpkg-output
          tree dist-deb
